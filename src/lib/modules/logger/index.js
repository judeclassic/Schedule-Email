class Logger {
    info = (message) => {
        console.log(message);
    }

    error = (message) => {
        console.log(message);
    }

    setUp = (message) => {
        console.log(message);
    }

    inform = (message) => {
        console.log(message);
    }
}

module.exports = Logger;